### oop.py 정리

캡슐화, 추상화

```
class Dog:
    def __init__(self, name, breed):
        self.name = name
        self.breed = breed

    def bark(self):
        return f"{self.name}가 멍멍 짖습니다!"

dog1 = Dog("바둑이", "진돗개")
print(dog1.bark())
```

`__init__ (초기화 메서드)` : 객체가 처음 만들어질 때 **자동으로 실행** 되는 함수

> 언제 실행되나요?: dog1 = Dog("바둑이", "진돗개")라고 쓰는 
> 순간, 파이썬 내부에서는 자동으로 __init__ 함수를 호출

`self` : 객체 자신을 가리키는 키워드

> **dog1을 만들 때는 self가 dog1**
> Dog라는 설계도는 하나지만, 앞으로 dog1, dog2, dog3처럼 수많은 강아지가 만들어질때, 구분해준다

---

### oop1.py 정리

(캡상추다 에서 쓰인것)

캡슐화(미비함)

상속 (Inheritance) -  가장 메인!

다형성 (Polymorphism) -  오버라이딩 덕분에!

```
class Animal:
    def __init__(self, name):
        self.name = name

    def speak(self):
        return f"{self.name}가 소리를 냅니다."

class Cat(Animal):
    def speak(self):  # 메서드 오버라이딩
        return f"{self.name}가 야옹 합니다."

cat1 = Cat("나비")
print(cat1.speak())
```

- **cat1 = Cat("나비") 이 실행되었을때의 실제 동작!**

> 핵심은 **"자식(Cat)이 __init__을 안 가지고 있으면, 부모(Animal) 것을 빌려 쓴다"**

__init__ 함수 찾기 (Search)
파이썬: "이제 초기화를 해야지. Cat 클래스, 너 __init__ 함수 있어?"

Cat 클래스: "아니요, 전 없는데요? (speak만 있어요)"

그럼 Animal의 __init__을 실행한다!"


>Animal.__init__(self, name)을 실행합니다.
>self 자리에는 -> 아까 만든 Cat 덩어리가 들어갑니다.
>name 자리에는 -> **"나비"**가 들어갑니다.

---

### oop2.py 정리

 **"캡슐화(Encapsulation)"** 를 설명하는 완벽한 예제

 > (상속, 추상화, 다형성)는 이 코드에 쓰이지 않았다.

```
class BankAccount:
    def __init__(self, owner, balance=0):
        self.owner = owner
        self.__balance = balance  # private 속성

    def deposit(self, amount):
        self.__balance += amount

    def withdraw(self, amount):
        if amount <= self.__balance:
            self.__balance -= amount
        else:
            print("잔액 부족!")

    def get_balance(self):
        return self.__balance

account = BankAccount("홍길동", 1000)
account.deposit(500)
account.withdraw(200)
print(account.get_balance())
```

- __balance  정보은닉

한마디로 "보기만 해! (Read-Only)" 입니다.

__balance는 밖에서 보이지 않는 금고 안에 들어있습니다.

만약 이 함수가 없다면?: 밖에서 잔액을 확인할 방법이 아예 없습니다. (철벽 보안이지만 너무 불편함)

그냥 self.balance로 공개하면 안 되나요?: 그러면 잔액을 확인할 수도 있지만, 몰래 바꿀 수도 있게 됩니다. (account.balance = 0 😱)

- **get_balance의 역할 (Getter):**

> 읽기는 허용해 줍니다. (return으로 값만 보여줌)
> 하지만 수정은 못 하게 막습니다. (값을 바꾸는 코드가 함수 안에 없으니까요)

**최종적으로 __ 는 클래스 내부에서만 수정가능**

---

### oop3.py 정리

**"추상화(Abstraction)와 다형성(Polymorphism)"**을 보여주기 위한 교과서적인 예

> 캡 추 다

추상화 (Abstraction) : 규칙만 정해두고, 실제 계산법은 비워뒀습니다(pass). 이것이 추상화입니다.

```
from abc import ABC, abstractmethod

class Shape(ABC):  # 추상 클래스
    @abstractmethod
    def area(self):
        pass

class Rectangle(Shape):
    def __init__(self, w, h):
        self.w, self.h = w, h

    def area(self):
        return self.w * self.h

class Circle(Shape):
    def __init__(self, r):
        self.r = r

    def area(self):
        return 3.14 * self.r * self.r

shapes = [Rectangle(3, 4), Circle(5)]
for s in shapes:
    print(f"도형의 넓이: {s.area()}")
```


다형성의 핵심
> shapes = [Rectangle(3, 4), Circle(5)]  # 서로 다른 객체들이 섞여 있음
```
for s in shapes:
    # ⭐️ 여기가 핵심!
    # s가 사각형이든 원이든 상관 안 하고 그냥 "넓이 내놔!"라고 한 가지 명령만 내림
    print(s.area())
```

>그냥 pass만 쓴 건 **"빈 껍데기 흉내"**일 뿐이고, @abstractmethod를 써야  >**"법적 효력이 있는 계약서(진정한 추상화)"**가 됩니다.

- 즉, 부모클래스와 자식클래스가 존재해야하고

부모클래스에 @abstractmethod가 있어야하고
> 부모클래스에서 꼭 선언되어야 함.!

자식클래스에서 정의를 꼭 해야한다
> 자식클래스에서 꼭 정의되어야 함.!