# 1회차 코드리뷰 - 객체지향 프로그래밍 (OOP)

> 2026.01.08 / DH & DW

---

## OOP 4대 원칙

| 원칙 | 파일 | 핵심 |
|------|------|------|
| 캡슐화 | oop.py, oop2.py | 데이터+메서드를 하나로 묶음 |
| 상속 | oop1.py | 부모의 기능을 물려받음 |
| 다형성 | oop1.py, oop3.py | 같은 메서드, 다른 동작 |
| 추상화 | oop3.py | 구현 강제, 설계도 역할 |

---

## 1. 클래스 기초 (oop.py)

```python
class Dog:
    def __init__(self, name, breed):
        self.name = name
        self.breed = breed

    def bark(self):
        return f"{self.name}가 멍멍 짖습니다!"

dog1 = Dog("바둑이", "진돗개")
```

### 핵심 개념

| 요소 | 설명 |
|------|------|
| `__init__` | 생성자 - 객체 생성 시 자동 호출되어 초기화 |
| `self` | 현재 인스턴스(나 자신)를 가리키는 참조 |
| `dog1` | Dog 클래스의 인스턴스(실제 객체) |
| `Dog("바둑이", "진돗개")` | 인스턴스 생성 → `__init__` 자동 호출 |

### 실행 흐름
```
Dog("바둑이", "진돗개") 호출
  → 메모리에 빈 객체 생성
  → __init__(self, "바둑이", "진돗개") 자동 호출
  → self.name = "바둑이", self.breed = "진돗개" 저장
  → 완성된 객체를 dog1에 할당
```

---

## 2. 상속과 다형성 (oop1.py)

```python
class Animal:
    def __init__(self, name):
        self.name = name

    def speak(self):
        return f"{self.name}가 소리를 냅니다."

class Cat(Animal):  # Animal 상속
    def speak(self):  # 메서드 오버라이딩
        return f"{self.name}가 야옹 합니다."

cat1 = Cat("나비")
```

### 핵심 개념

| 개념 | 설명 |
|------|------|
| `Cat(Animal)` | Cat은 Animal을 상속 (부모 기능 물려받음) |
| 오버라이딩 | 부모 메서드를 자식이 재정의 |
| 다형성 | 같은 `speak()` 호출, 다른 결과 |

### Cat("나비") 실행 시 동작
```
1. Cat에 __init__ 있나? → 없음
2. 부모(Animal)의 __init__ 실행
3. self.name = "나비" 저장
4. cat1.speak() → Cat의 speak() 실행 (오버라이딩)
```

### 부모 생성자 호출 (super)
```python
class Cat(Animal):
    def __init__(self, name, color):
        super().__init__(name)  # 부모 __init__ 호출
        self.color = color      # 자식만의 속성
```

---

## 3. 캡슐화와 정보 은닉 (oop2.py)

```python
class BankAccount:
    def __init__(self, owner, balance=0):
        self.owner = owner
        self.__balance = balance  # Private 속성

    def deposit(self, amount):
        self.__balance += amount

    def withdraw(self, amount):
        if amount <= self.__balance:
            self.__balance -= amount
        else:
            print("잔액 부족!")

    def get_balance(self):  # Getter
        return self.__balance
```

### 핵심 개념

| 표기법 | 접근 수준 | 설명 |
|--------|----------|------|
| `name` | Public | 어디서든 접근 가능 |
| `_name` | Protected | 관례상 내부용 (접근은 가능) |
| `__name` | Private | 외부 접근 차단 |

### 왜 Private을 쓰는가?
```python
# Public일 때 문제점
account.balance = -1000000  # 😱 데이터 무결성 파괴

# Private일 때
account.__balance = -1000000  # ❌ 접근 불가
account.withdraw(100)         # ✅ 검증 로직을 거침
```

### Name Mangling (이름 변환)
```python
# Python은 __속성을 완전 차단하지 않고 이름 변환
account._BankAccount__balance  # 접근은 가능 (비권장)
```

### Getter의 역할
- **읽기만 허용**, 수정은 메서드(deposit, withdraw)를 통해서만

---

## 4. 추상화 (oop3.py)

```python
from abc import ABC, abstractmethod

class Shape(ABC):  # 추상 클래스
    @abstractmethod
    def area(self):
        pass

class Rectangle(Shape):
    def __init__(self, w, h):
        self.w, self.h = w, h

    def area(self):
        return self.w * self.h

class Circle(Shape):
    def __init__(self, r):
        self.r = r

    def area(self):
        return 3.14 * self.r * self.r

shapes = [Rectangle(3, 4), Circle(5)]
for s in shapes:
    print(f"도형의 넓이: {s.area()}")
```

### 핵심 개념

| 요소 | 설명 |
|------|------|
| `ABC` | 추상 클래스 생성을 위한 부모 클래스 |
| `@abstractmethod` | 자식이 반드시 구현해야 하는 메서드 |
| 추상 클래스 | 직접 인스턴스화 불가, 설계도 역할 |

### 추상 클래스의 규칙
```python
shape = Shape()  # ❌ TypeError! 직접 생성 불가

# 자식이 area()를 구현 안 하면?
class Triangle(Shape):
    pass

tri = Triangle()  # ❌ TypeError! 추상 메서드 미구현
```

### 왜 추상 클래스가 필요한가?
1. **계약 강제** - 자식은 반드시 해당 메서드 구현
2. **실수 방지** - 미구현 시 실행 전에 에러 발생
3. **다형성 보장** - 모든 Shape가 `area()` 보유 확정

### 다형성의 힘
```python
# 어떤 도형이든 같은 코드로 처리
for s in shapes:
    print(s.area())  # Rectangle → 12, Circle → 78.5
```

---

## 정리: pass vs @abstractmethod

| 구분 | `pass`만 사용 | `@abstractmethod` |
|------|--------------|-------------------|
| 구현 강제 | 🔺 안 함 | 🔹 강제 |
| 에러 시점 | 런타임 (실행 중) | 인스턴스 생성 시 |
| 용도 | 빈 껍데기 | 법적 효력 있는 계약 |

---

## 한눈에 보기

```
oop.py   → 클래스, __init__, self, 인스턴스
oop1.py  → 상속, 오버라이딩, super()
oop2.py  → Private(__), Getter, 정보 은닉
oop3.py  → ABC, @abstractmethod, 추상화
```
